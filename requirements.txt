# ---------------------------------------------------------------------------
# TAFAHOM — shared Python dependencies
# ---------------------------------------------------------------------------
# Experiment-specific extras live in experiments/*/requirements.txt and pull
# this file in via `-r ../../requirements.txt`.
#
# Versions are pinned with lower bounds; pin exactly for full reproducibility.

# Core numerics
numpy>=1.24
scipy>=1.10
pandas>=2.0

# Deep learning
torch>=2.1
torchvision>=0.16

# Landmark extraction / video IO
mediapipe>=0.10
opencv-python>=4.8

# Configuration & utilities
pyyaml>=6.0
tqdm>=4.66

# Evaluation & visualization
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.13

# Inference / serving
fastapi>=0.110
uvicorn>=0.29
