name: tafahom
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip>=24.0
  - numpy>=1.24
  - scipy>=1.10
  - pandas>=2.0
  - scikit-learn>=1.3
  - matplotlib>=3.7
  - seaborn>=0.13
  - pyyaml>=6.0
  - tqdm>=4.66
  - pip:
      # Installed via pip for the most current wheels / GPU builds.
      - torch>=2.1
      - torchvision>=0.16
      - mediapipe>=0.10
      - opencv-python>=4.8
      - fastapi>=0.110
      - uvicorn>=0.29
